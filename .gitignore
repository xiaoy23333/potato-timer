# 依赖：380MB，绝对不要提交（有 package.json + pnpm-lock.yaml 就能还原）
node_modules/
.pnpm-store/

# 自检产物：截图里有你的桌面内容（浏览器标签、其它窗口），而且随时能重新生成
screenshots/

# 打包产物：安装包 / 免安装版 / 解包目录，几百 MB，随时能重新生成
dist/

# 临时日志
*.log
smoke-*.log
smoke-*.err

# 与本项目无关：改 DSH 输入框快捷键 / 验签用的脚本与备份，别混进番茄钟的仓库
patch-enter-key.ps1
verify-chain.ps1
chain.test.js
chain.test.js.orig

# 系统 / 编辑器
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/
*.swp
