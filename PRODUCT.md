# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

> 说明：这是一个 **Electron 桌面应用**（Windows 优先），界面用原生 HTML/CSS/JS 渲染。
> 四个可选值（web / ios / android / adaptive）里没有「桌面」这一项，取 `web` 是因为它的
> 设计语言确实是 Web 技术，且不需要加载 iOS / Android 的原生平台指引。
> 但**它不是网站**：有托盘、全局快捷键、无边框置顶窗口、开机自启这些桌面形态。
> 后续任何设计工作都不要把它当成一个网页来对待。

## Users

**主要用户：开发者本人 + 身边少数几个人（朋友 / 同事）。**

- 场景：Windows 桌面上的日常办公 / 写代码 / 学习，坐在电脑前长时间专注。
- 要干的活：把一大块工作时间切成一段段专注，并在每段结束时**被明确地打断一次**，
  由本人决定「接下来干什么」。
- 这个规模决定了后面**不需要**：新手引导流程、多语言、代码签名、商店上架、遥测。
  **需要**的是：别人拿到之后能装上、能跑起来、出问题时看得懂提示。

## Product Purpose

一个跑在本机的番茄钟。它把工作时间切成专注段与休息段，**在专注段结束的那一刻用一个
无法忽视、也不会替你做决定的提醒把人叫住**。

成功的样子：一天下来，每一段专注都是被完整走完的；段与段之间的切换是使用者自己按下的；
在提醒之外的时间里，几乎想不起来电脑上还开着它。

## Positioning

**「到点提醒」那一下，就是它非存在不可的理由。**

主流的番茄钟把注意力花在任务清单、专注统计、番茄森林、社交排行上，而提醒本身往往只是
一条会自己消失的系统通知。这个产品反过来——**提醒的质量是全部**：

1. 屏幕四周向内的光效，**盖在所有窗口之上**（含全屏应用）；
2. 屏幕顶部一个**不会自己退回去**的弹窗；
3. 弹窗上只有两个出口，且都**必须由使用者亲手做出**：
   - 延后一段固定时间（按钮或全局快捷键，可无限次）；
   - 进入下一轮循环（把滑块**拖到最右端**才触发）。

它不是「又一个番茄钟」，而是「一个把『时间到了』这件事做对的小工具」。

## Operating Context

- **环境**：Windows 桌面；使用者自己的机器上是**单屏**；长时间挂着不关。
- **形态**：开机自启后静默进托盘；主窗口是常规窗口（环形进度 + 大号倒计时 + 设置面板）；
  另有一个常驻小浮窗贴在屏幕正上方，只显示时间。
- **提醒时的物理事实**：四周光效**点击穿透**（不挡任何操作）；弹窗**不抢键盘焦点**
  （正在打字、正在全屏游戏时不会被打断输入）；小浮窗在提醒期间让位。
- **不打扰是硬要求**：提醒之外的一切都必须退到背景里。
- **数据**：所有配置只存在本机（Electron 用户数据目录下的 `config.json`）。
  这个应用**没有任何网络代码**。

## Capabilities and Constraints

### 已交付的能力

- 计时循环：默认 40 分钟专注 / 5 分钟短休 / 每 4 轮 10 分钟长休，**全部可配置且精确到秒**；
  休息时长设为 `0` 即跳过休息直接进入下一轮专注。
- 提醒三件套：四周向内光效（只闪一次后自己淡出）+ 顶部不自动消失的置顶弹窗 + 两个出口。
- 全局快捷键 `Ctrl+Alt+P` = 延后（可自定义，冲突时提示换一个）。
- 主窗口 / 小浮窗 / 托盘 / 设置面板 / 开机自启 / 合成提示音。
- 暂停、继续、跳过、重置；改时长对正在跑的这一轮立即生效。
- 休眠、睡眠、锁屏自动暂停，唤醒接着跑（不计入离开电脑的时间）。
- 单实例；关闭窗口 = 退出程序，最小化 = 进托盘继续计时。

### 技术约束

- Electron 44 + 原生 HTML/CSS/JS，**无打包器、无框架、零构建**。
- 计时状态机跑在**主进程**，窗口隐藏 / 托盘期间不受渲染层节流影响。
- 界面交互的验证走真实输入管线，并有**操作系统真鼠标**的回归断言。

### 尚未计划的功能（不是禁地，只是本轮没做）

任务标签、历史统计面板、深色主题、云同步、账号体系。

## Brand Commitments

- **名字**：中文「番茄钟」，英文 / 仓库名 `PomodoroTimer` / `potato-timer`。
- **语言**：界面文案、按钮、提示**全部中文**。
- **淡蓝色四周向内光效**：这是需求方明确选定、并明确要求**不要改动**的品牌承诺。
  它的颜色由设置里的 `glow.color` 驱动，默认 `#4fc3f7`。
- **语气**：直白、说人话。控件说清自己的动作，出错时说清问题和解法，不卖萌、不喊口号。

## Evidence on Hand

- `计划.md` —— 需求与决策的唯一事实来源：锁定的需求（第 1 节）、状态机（第 2 节）、
  技术方案（第 3 节）、替换我做的默认决定（第 4 节）、进度（第 5 节）、
  bug 根因（第 6 节）、工作流程与验证规范（第 7 节）、**配色系统（第 8 节）**。
- `未解决问题.md` —— 反复修不好的问题 / 我需要人工确认的事项 / 已解决记录，
  外加「给未来的自己：踩过的工具坑」。
- `README.md` —— 面向使用者的安装与操作说明。
- 验证材料：`scripts/test-timer.js`（计时状态机单元测试）、
  `POMODORO_E2E=1`（端到端自检，含真实截图与像素差分）、
  `scripts/audit-colors.js`（对比度审计）、`scripts/shot-palette.js`（配色验收出图）。
- 仓库：`https://github.com/xiaoy23333/potato-timer`（公开），标签 `v0.1.0`。

### 明确不存在的材料（后续工作**不得编造**）

- **没有 `LICENSE` 文件**，但 `package.json` 里写了 `"license": "MIT"`。
  这是一个真实缺口：想清楚再定许可证，不要凭空生成一份。
- 没有 `author` 字段，没有图标授权说明。
- 没有用户数、下载量、评测、媒体报道、客户证言——**一个都没有，不要引用任何数字**。
- 没有设计稿、Figma 文件或视觉规范文档（`DESIGN.md` 尚未建立）。

## Product Principles

1. **提醒那一刻就是产品本身。** 其他所有功能都为那一下服务；在那一下之外，
   它应当退到背景里。
2. **提醒不许自己消失，也不许替使用者做决定。** 它只提供出口，选择必须由人按下。
3. **显眼，但不夺走控制权。** 光效点击穿透、弹窗不抢焦点——提醒要盖得住一切，
   但绝不能挡操作、抢输入。
4. **状态要一眼可读。** 当前是什么阶段、还剩多久、下一段是什么，都不用猜。
5. **别人拿到也要能跑起来。** 因为要给身边人用：能装上、能看懂、出错知道怎么办。
