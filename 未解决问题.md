# 未解决问题与待确认项

> 配套 `计划.md` 第 7 节的工作流程使用：凡是**反复修不好**、或者**我无法自动验证**的事项，都记在这里，不在主流程里死磕。
>
> 规则：本文件**只增不减** —— 问题解决了不删，标注解决方式后挪到第三部分。
>
> 最后更新：2026-09-14

---

## 一、反复修不好的问题

> 判定标准：同一问题**连续两轮「修改 + 验证」仍未解决**。
> 记录时必须写清：现象、根因猜测、已经试过什么、下一步思路。

| 编号 | 问题 | 现象 | 根因猜测 | 已经试过 | 下一步思路 |
| --- | --- | --- | --- | --- | --- |
| S1 | 自检里「操作系统真鼠标能点到弹窗」偶发全失败 | 某一次连跑 3 次尝试 `pointerdown` 全是 0，而之后连跑 7 次又全过（约 1/8 的概率）。同一次运行里其它断言都正常 | **未确定。** 候选：① 注入本身偶发打空（光标刚移过去、点击还没投递）；② 那个满屏置顶的光效窗口的合成器太忙（自检里刚跑过一段 rAF 探针），拖慢了鼠标消息的转发；③ 弹窗的输入区域修复（显示后 130ms 挪 1px）偶发没生效 | ① 加了 3 次重试 —— **没解决**，说明不是单纯的注入噪声（单纯打空重试一次就该过）；② 把新增的 rAF 探针从最多 400 帧收到 150 帧，避免它在后面拖后腿；③ 加了落点诊断，失败时会打印"该点命中了哪个元素"，下次复现就能一眼分清是坐标问题还是窗口收不到鼠标 | 下次再复现时先看落点诊断：如果命中 `<slider>` 却仍然 `pointerdown=0`，那就是**窗口层**的问题（候选 ②/③），接着量「点击那一刻弹窗的 bounds 和可见性」；如果命中 `null` 或别的元素，就是**坐标/遮挡**的问题 |

---

## 一之二、连续两轮判错、第三轮才修好的问题

> 单独立一节，因为这类问题的教训和"技术修不好"不一样 —— 技术上是修得好的，是我**判断错了该不该修**。

| 编号 | 问题 | 前两轮怎么判错的 | 第三轮怎么查出来的 | 真正的教训 |
| --- | --- | --- | --- | --- |
| J1 | 光效「先比屏幕小一圈、再放大到屏幕大小」 | **第一轮**：量了 alpha 剖面，证明几何恒定、边缘无空隙 → 结论"这是柔光的固有观感，不是 bug"，只留一句"需要的话说一声"。**第二轮**：需求方再提，我用 `diagnose-glow-grow.js` 逐帧量"差异像素的外接矩形"，它只要最外圈有任意一个像素超阈值就顶到屏幕边缘，于是又得出"铺满了、没变小" | 第三轮需求方补了关键线索「外圈切口是整齐的，像是被等比缩小了」。换成量**alpha 剖面随时间的形状**：几何确实恒定，但「alpha × 不透明度 > 眼睛阈值」这条可见边界会随亮度往里缩 —— 实测可见宽度变化 **2.31 倍**。修法：把不透明度下限抬到 0.62、渐变 84% 处落零，降到 **1.000 倍** | ① **"几何没变" ≠ "没问题"**：需求方说的是观感，我答的是几何。正确的问题不是"它到底变没变"，而是"怎么让它看起来不再变"。② **判错的诊断脚本要标注**，否则下次还会照着它得出同样的错结论（已在脚本头部加警告）。③ 同一个"光效变小"的问题被报告了三次，前两次都该当成 P0，而不是"需要的话说一声" |

---

## 二、尚未验证 / 待人工确认

> 这些**不一定是 bug**，而是自动化手段够不到的地方。我这边只能用像素差分、DOM 断言、窗口属性做间接证明，最终要你确认。

| 编号 | 项目 | 我已经验证到什么程度 | 需要你怎么确认 |
| --- | --- | --- | --- |
| U1 | 到点提醒的**视觉效果**好不好看 | 像素差分证明淡蓝光效确实盖在屏幕上（提醒前后对比：桌面左右边缘 Δ红 −30.7、Δ(蓝−红) +32.2），弹窗卡片区域像素差 20.3 说明卡片确实贴上了。但「好不好看」我判断不了 —— 当前模型读不了图片 | 看 `screenshots/04-桌面实际效果-提醒中.png` 和 `01-提醒弹窗.png`，颜色 / 强度 / 弹窗大小哪里不对就说 |
| U2 | 提示音**好不好听、够不够响** | 需求方反馈第一版「有点刺耳」。已按声学原因重新调音：基频从 E6(1318Hz) 降到 A5(880Hz)→E5(659Hz)、砍掉接近 4kHz 的 3 倍泛音、过 1.5kHz 低通、起音从 14ms 放宽到 45ms、总音量从 0.5 降到 0.38。**但我听不到声音**，好不好听只能你判断 | 再听一次（到点，或设置里点「试听提示音」）。还刺耳就说「再柔一点」，还想更清脆我就把低通放开一些 |
| U3 | 光效层是不是**真的点击穿透** | 代码里调用了 `setIgnoreMouseEvents(true, {forward:true})`，但 Electron 没有提供读取该状态的接口，断言不了 | 提醒出现时故意点一下屏幕边缘，看鼠标有没有被吃掉、能不能正常点到下面的窗口 |
| U4 | ~~弹窗的滑块用真鼠标能不能拖动~~ | **已解决**（见第三部分）—— 用操作系统真鼠标完整验证通过 | — |
| U10 | 光效**闪一次后消失**的观感对不对 | 自检断言了「动画只播 1 次、动画名是 flash、闪光结束后 opacity 回到 0」，也就是**逻辑上确认会自己消失**。但 1.6 秒内那几下亮暗的节奏好不好看、够不够抓眼，我判断不了 | 跑起来看一次提醒：觉得闪得太快/太慢就调设置里的「闪光时长」；觉得不够明显就调「强度」 |
| U11 | ~~滑块「第一次能拖、后面拖不动」~~ | **已解决**（见第三部分）—— 根因不是滑块，是**整个弹窗窗口在重新显示后丢掉了鼠标输入区域**，已修复并有真鼠标回归断言 | — |
| U5 | 能不能盖住**真正的全屏应用**（游戏 / 全屏视频） | 代码用的是 `setAlwaysOnTop(true, 'screen-saver')`，但没有真实全屏应用可供测试 | 开个全屏视频或游戏，等提醒弹出来看能不能盖住 |
| U6 | **休眠 / 锁屏**后是否真的暂停、醒来接着跑 | 状态机的 `onSystemSuspend` / `onSystemResume` 有单元测试覆盖，但没法在你这台机器上真的休眠一次 | 合盖或锁屏几分钟再回来，看倒计时有没有被「偷走」 |
| U7 | **开机自启**是不是真的能起来 | 已修正为「未打包时把应用目录一并写进启动项」（否则开机项只指向 electron.exe，起不来）；但需要重启才能确认 | 下次开机看托盘里有没有番茄钟。不想要就在设置里关掉 |
| U8 | 托盘图标在 **16px** 下清不清楚 | 图标是用 `scripts/make-icons.js` 代码画的番茄，托盘里会缩到 16×16 显示 | 看任务栏右下角托盘图标 |
| U9 | ~~提醒弹出时会不会打断你正在做的事~~ | **已解决**（见第三部分） | — |
| U12 | **主页面新布局好不好用** | 这次重做是需求方说「不满意但也没想好怎么改」之后我主动改的一版。几何、对比度、状态自洽我都能量（见 `计划.md` 第 10.5 节），但「顺不顺眼」判断不了 | 直接跑起来看一眼（`pnpm start`）。重点看三处：① 顶部那行「阶段在左、轮次在右」会不会显得像多了一条工具栏；② 跑起来时主按钮从实心红变成红描边，接不接受；③ 待开始那一屏只有一个大数字 + 一个实心红按钮，是不是太空。**任何一处不喜欢都可以单独回退**，说一声就行 |
| U13 | 休息 / 延后 / 暂停三种状态的**颜色区分**够不够明显 | 现在是三条规则：专注中＝番茄红、休息和延后＝青蓝、暂停＝纯灰。三种状态我都出了截图 | 看 `screenshots/主界面-03-已暂停.png`、`04-短休进行中.png`、`07-延后中.png`。暂停已按你的要求改成纯灰 |
| U14 | 主界面的**留白够不够**了 | 窗口 384×548 → 420×640，表盘上限反而从 460 收到 300；左右视觉留白 54→73px，上下 10→56px（都是实测值，见 `计划.md` 11.1） | 跑起来看一眼。还是嫌满就继续加内边距 / 收表盘；觉得空就把表盘上限调回大一点。两个方向都是改一个数 |
| U15 | 光效现在**够不够亮、节奏对不对** | 修法是"3% 处就把亮度抬到 0.62"（1.6 秒下约 48ms 到位），所以现在起得比原来快得多。实测可见宽度全程恒定（1.000 倍），不会再"长大" | 看一次提醒。**注意这次改的是"起得多快"**：如果觉得太突兀，就把设置里「闪光时长」调长一点（起点会等比变慢）；如果还嫌不够抓眼，就调「强度」 |

---

## 三、已解决（保留记录）

| 编号 | 问题 | 解决方式 | 解决时间 |
| --- | --- | --- | --- |
| — | 计时器永远跑不完（配置对象引用过期） | `Store.set()/reset()` 改为 `Object.assign` 就地更新同一对象 | 2026-09-13 |
| — | 长休永远不出现（多算一轮） | 拆成 `breakDueAfterFocus()` 与 `predictedBreak()` 两个函数 | 2026-09-13 |
| — | 点延后后弹窗不收起 | `main.js` 里「是否仍在提醒」只认 `finished`，不再把 `snoozed` 算进去 | 2026-09-13 |
| — | 小浮窗永远不显示 | 在 `ready-to-show` 里调 `showInactive()` | 2026-09-13 |
| — | 光效底部一条不亮（被裁到工作区） | 创建后再调一次 `setBounds` 校正 | 2026-09-13 |
| — | 自检会污染用户配置文件 | 自检期间停掉 `store._write`，收尾还原 | 2026-09-13 |
| — | 开发模式开机自启起不来 | 未打包时把 `app.getAppPath()` 写进启动项参数 | 2026-09-13 |
| — | 自检延后倒计时变成 01:00 导致断言崩 | 自检包一层 `store.set`，走完真实 set 流程后把测试用的极短时长盖回去（测试夹具问题，非产品 bug） | 2026-09-13 |
| — | **提醒弹出会不会抢键盘焦点**（原 U9） | 自检里加了一条**行为级**验证：先把焦点给主窗口，提醒弹出后主窗口仍持有焦点（`弹出前 true → 弹出后 true`），且弹窗自身 `isFocused() === false` | 2026-09-13 |
| — | 暂停中改短时长卡在「已暂停 00:00」 | `remaining <= 0` 时直接 `_finishPhase()` | 2026-09-13 |
| — | 待开始点「跳过」会凭空开跑一段休息 | `skip()` / `nextCycle()` 在 IDLE 下直接返回；「跳过」按钮同时置灰 | 2026-09-13 |
| — | 启动竞态下小浮窗与提醒弹窗顶部重叠 | 浮窗 `ready-to-show` 里增加「弹窗正在显示」守卫 | 2026-09-13 |
| — | 托盘标签「开始 / 继续」实际是「进入下一轮」 | 文案与动作都按状态分派 | 2026-09-13 |
| — | 改快捷键失败会**静默丢掉旧快捷键** | 先注册新键，成功了再注销旧键；失败时旧键保持有效 | 2026-09-13 |
| — | `hotkey:set` 未校验渲染层传入值 | IPC 入口先做类型 / trim / 长度校验 | 2026-09-13 |
| — | 心跳定时器不清理、广播漏查 `webContents`、重建窗口丢初始化 | 新增 `dispose()`；`broadcast` 补检查；新增 `onMainReady` 钩子；删掉死代码 | 2026-09-13 |
| — | 设置里清空数字输入框被当成 0 提交（会毁掉时长设置） | 先判空值与 `validity.badInput`，回填原值并提示 | 2026-09-13 |
| — | 快捷键录制不随离开设置页结束（会静默改键并吞键盘） | 切页 / 失焦 / 隐藏时统一 `stopRecording()` | 2026-09-13 |
| — | 滑块相对拖动：从轨道中间按下要拖出窗口才能到底 | 按下时把滑块贴到指针底下，并加「至少移动 8px 才触发」防误触 | 2026-09-13 |
| — | 拖动被打断后 `dragging` 不复位（可能单击就触发） | 指针事件挂到 `window`，补 `lostpointercapture`/`blur`/`visibilitychange` 兜底；状态离开「待处理」强制复位 | 2026-09-13 |
| — | 完成第 4 轮时进度点全灭 | 满一轮循环时显示「全亮」 | 2026-09-13 |
| — | **滑块拖不动**（需求方反馈） | 用操作系统真鼠标（`SetCursorPos` + `mouse_event`）完整复测：`pointerdown=1 / pointermove=3 / pointerup=1`、落点 `slider`、滑块走到 `244px = maxLeft`、文案变「开始下一轮」—— **滑块本身是好的**。最可能的原因是需求方测的是「相对拖动」缺陷修复之前的版本。排查过程中的误判与教训见 `计划.md` 第 6.2 节 | 2026-09-13 |
| — | 时长只能精确到分钟 | 内部改为一律按**秒**存（`focusSeconds` 等），设置面板改成「分 + 秒」两个输入框；旧版按分钟存的配置在 `store.normalize` 里自动换算 | 2026-09-13 |
| — | 光效一直在闪 | 从 `infinite alternate` 的呼吸脉动改成**一次性闪光后常亮**（`animation: flash … 1 both`），设置项也从「呼吸速度」改成「闪光时长」 | 2026-09-13 |
| — | 小浮窗有白色胶囊底 | 背景改为**完全透明**，只留时间文字 + 文字阴影 | 2026-09-13 |
| — | 提示音刺耳 | 降基频、砍高频泛音、加 1.5kHz 低通、放宽起音（见 U2）。需求方复测：**目前可以** | 2026-09-13 |
| — | 光效「一直存在、不会自己消失」 | 上一版把「只闪一次」理解成了「闪完常亮」。改成**闪一下后自己淡出消失**（keyframes 结束在 opacity 0），并新增 `glow:play` 通道 —— 光效窗口是常驻的，每次显示时主进程显式通知页面重播闪光，否则只有第一次会闪 | 2026-09-13 |
| — | 滑块「第一次能拖、后面拖不动」 | **找到真根因**：不是滑块的问题，而是整个弹窗窗口在 `hide() → showInactive()` 之后**丢掉了鼠标输入区域**（Electron 在 Windows 上对「透明 + 置顶 + focusable:false」窗口的已知行为）。修法：显示后等 130ms 把窗口挪 1px 再挪回来，强制系统重建输入区域。对照实验与全过程见 `计划.md` 第 6.4 节 | 2026-09-13 |
| — | 自检抓不到上面那个 bug | 自检的界面交互全走 `sendInputEvent`，它是直接注入渲染进程的，**绕过操作系统的窗口命中测试**。已补上「用操作系统真鼠标点一次弹窗」的回归断言 | 2026-09-13 |
| — | 小浮窗倒计时的阴影太重 | 原来是三层叠加（其中一层还是 16px 的大范围模糊），减到两层很淡的阴影 | 2026-09-13 |
| — | 光效「先比屏幕小一圈、再扩大到四周」 | 量了 alpha 剖面，**几何上没有任何变化**（形状恒定、边缘无空隙）。是柔光 + 亮度变化造成的观感。但顺带发现关键帧其实是**闪了两下**（`0→1→0.55→1→0`），与「只闪一次」不符 —— 已改成一次干净的闪光 `0→1→1→0`。详见 `计划.md` 第 6.5 节 | 2026-09-13 |
| — | 自检写死用户可改的默认值，报了一条假失败 | 断言小浮窗颜色等于默认番茄红，而需求方早已改成淡蓝。改为**从配置里读出来再比**；截图/像素差分这类对参数敏感的检查，先把参数钉成已知值、做完再还原 | 2026-09-13 |
| — | **休息时进度环整条消失** | `index.js` 里写的是 `var(--break)`，而配色重做时这个令牌已经改名成 `--rest`。未定义的 `var()` 让 `stroke` 在计算值阶段失效、退回初始值 `none`。改为 `var(--rest-strong)`，并补了 3 条回归断言（已做反向对照） | 2026-09-14 |
| — | **延后中的进度弧算错** | 拿 `snoozeRemainingMs` 去除以 `s.totalMs`（专注总时长）。5 分钟延后配 40 分钟专注时画出来是一条几乎满圈的弧，和屏幕上的 `03:20` 对不上。改为按延后的总时长算，并补了 1 条「弧长与环里数字自洽」的回归断言（已做反向对照） | 2026-09-14 |
| — | 休息时轮次刻度还是专注红 | `.dots i.is-done` 硬编码 `--focus`。改为跟随阶段切换（休息用 `--rest-strong`，延后中退成中性） | 2026-09-14 |
| — | 状态徽章文字偏心 1px | `letter-spacing` 在最后一个字后面也留一份字距，居中时整体偏左。内边距改成不对称的 `0 10px 0 14px` 补偿 | 2026-09-14 |
| — | 主界面「占得太满、四周想多留白」 | 窗口 384×548 → 420×640，表盘上限 460 → 300。关键是**窗口变大、表盘反而略小**，多出来的空间才真的变成留白。另修：`mainWindowBounds` 里若存着旧版默认尺寸，会被识别成"用户从没调过窗口"，只沿用位置 | 2026-09-14 |
| — | 暂停时的进度环是灰绿色 | `--ink-faint` 是从品牌青蓝推导的（G > R 且 B > G），确实读得出灰绿。新增 `--idle-ring: #6d6d6d` 纯灰，亮度刻意对齐原来的 4.7:1。**这是对"中性色一律从品牌青蓝推导"这条硬规则的点名例外**，已在 `theme.css` 写明原因 | 2026-09-14 |
| — | **光效「先比屏幕小一圈、再放大」**（需求方第三次反馈） | 见第一部分 J1。几何恒定（逐帧探针：视口与四边尺寸全程只有 1 种值），但**可见宽度**会随亮度变化 —— 实测不透明度 0→1 时变化 **2.31 倍**。修法：`@keyframes flash` 在 3% 处就把不透明度抬到 0.62（1.6 秒下约 48ms 到位），`.edge-*` 渐变从 100% 收到 84% 落零 → 实测降到 **1.000 倍**。新增 2 条回归断言（闪光全程视口恒定 / 光效层尺寸恒定）+ 1 条确定性曲线断言 | 2026-09-14 |

> 完整的 bug 根因分析见 `计划.md` 第 6 节（含 6.1 对抗性审查、6.2「滑块拖不动」的排查过程、6.4 窗口输入区域、6.5 光效观感）。

---

## 四、给未来的自己：这次踩过的工具坑

- **自检里不要写死「用户可改的默认值」。** 我断言小浮窗颜色等于默认番茄红 `#f4664f`，而需求方早就把它改成了淡蓝 `#52d9f4`，于是自检报了一条**假失败**。凡是跟用户设置有关的断言，都要**从配置里读出来再比**；对参数敏感的截图 / 像素差分，要**先把参数钉成已知值、做完再还原**。
- **`webContents.sendInputEvent` 会绕过操作系统的窗口命中测试。** 界面交互用它做断言是稳定、可重复的，但**它抓不到「窗口收不到鼠标」这类系统层问题** —— 那个 bug 让自检一路 66/66 全绿却是坏的。凡是涉及"窗口到底收不收得到鼠标"，必须另配一个**操作系统真鼠标**的探针。
- **用 PowerShell 注入真鼠标时，`SetCursorPos` / `mouse_event` 吃的是「虚拟化逻辑坐标」，不是物理像素。** 坐标一律用 `getBounds()` 的逻辑值，**不要乘 `scaleFactor`**。这台机器上物理 2560×1440、逻辑 2048×1152，乘错一次就让所有结论偏 1.25 倍，白排查了 8 轮。
- **`WindowFromPoint` 返回的区间不能直接当成窗口边界**，除非你确定自己用的坐标系和它一致。
- **"修法对但没生效" 先怀疑时机。** 这次的修法（显示后再 `setBounds` 一次）单独测有效，但写进 `showPopup()` 里同步执行就无效了 —— 必须**错开 130ms**。
- **统计 `Process` 时先按 `Path` 过滤。** DSH 自己也有 `electron.exe` 在跑，不区分就会把无关进程算成「我们的应用没退干净」。
- **绝对不要用 `Get-Content -Raw | Set-Content` 去改任何带中文的文件。** 我拿它做了一次「把 `var(--rest-strong)` 换成 `var(--break)`」的临时改动，回来发现整个 `index.js` 的中文注释全变成乱码（`设置面�?`），连换行都被吃掉 —— 那一轮端到端自检红了 10 项，看上去像"断言抓到了 bug"，实际上是我的破坏性操作把文件写坏了，**那次验证结果完全无效**。更讽刺的是：写这段教训的时候我又用同样的法子去改本文档的日期，把它也写坏了，只能 `git checkout` 重来。**结论：改文件一律用编辑器工具做字面替换，一次也别碰 PowerShell 的文本管道**（哪怕是"只是改个日期"）。
- **反向对照（把 bug 改回去，确认断言真的会红）是必要的，但必须「只改这一处」。** 上面那次就是反面教材：文件一坏，10 项断言一起红，看起来声势浩大，其实什么也证明不了。有效的对照是**只红那一条**，而且错误信息里的值恰好就是预告的那个值 —— 这次两条都做到了：`→ none`、`offset=603.2`。
- **别对着挂钟写断言。** 「等 2.9 秒，进度应该到一半」这种写法机器一忙就随机红（状态每秒才广播一次，差出一秒很正常）。改成对着**界面自己给出的两个数字**断言（环的弧长 vs 环里的时间）—— 它们由同一次渲染写入，天然自洽，跟机器快慢无关。第一版写成挂钟断言连红两次，我差点去查根本不存在的代码 bug。
- **别用眼睛量几何。** 「这个间距是不是偏了」「数字会不会溢出环内圈」这类判断，`getBoundingClientRect()` 一次就给准数。`scripts/shot-main.js` 里的几何体检就是为此加的（4 种窗口尺寸 × 环径 / 字号 / 数字宽 / 环内圈 / 上下留白）。我靠肉眼估，估错了 3 次。
- **`npx impeccable` 会去装 npm 上一个同名的无关包。** 真正的 CLI 在技能目录里：`$env:USERPROFILE\.agents\skills\impeccable\scripts\impeccable.cmd`。
- **"几何没变"不等于"没问题"。** 需求方说光效"先是比屏幕小一圈"，我量了三次几何、三次都证明没变，于是三次都说"这是观感不是 bug" —— 而需求方要的是**看起来别再变小**。**用户报的是观感时，别拿几何去反驳他**；几何只是用来找原因的，不是用来结案的。
- **失败的诊断脚本一定要在头部标注，否则它会一直误导未来的你。** `diagnose-glow-grow.js` 量的是"差异像素的外接矩形"，只要最外圈有任意一个像素超过阈值它就顶到屏幕边缘，于是永远输出"铺满了、没变小"。我在它头部加了 ⚠ 警告和正确仪器的指路（`verify-glow-flash.js`）。**留着不标注比删掉更糟。**
- **采样的帧率会骗人。** 我把 rAF 探针的"最多 400 帧"当成"整段闪光都采到了"，其实 400 帧在 60fps 下是 6.7 秒，在机器忙的时候帧率掉下来，同一个循环会一直跑下去、拖累后面的断言。**别用帧数当时间用**，要覆盖一段时间就写时间。
- **"差异像素的阈值"在真实壁纸上极不可靠。** 同一圈光，壁纸亮的地方差 30、暗的地方差 3，阈值设 10 就只捞得到零星几个像素，外接矩形纯属噪声。要在真实桌面上量光效，量 **Δ(蓝−红)** 这类"颜色偏移"比量亮度稳得多；能只量光效那一层自己的 alpha 就更稳（`capturePage` 光效窗口）。
